---
version: 5
defaults:
  datadir: /opt/puphpet
  data_hash: yaml_data

hierarchy:
  - name: YAML data
    paths:
      - "config-%{::provisioner_type}.yaml"
      - /vagrant/puphpet/config-custom.yaml
      - config.yaml
